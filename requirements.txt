# Core (live agent + retrieval)
anthropic>=0.40.0
voyageai>=0.3.0
python-dotenv>=1.0.0
chromadb>=0.5.0

# Ingestion pipeline (parsing, chunking, token counting)
pypdf>=4.0.0
pypdfium2>=4.0.0
tiktoken>=0.7.0
openpyxl>=3.1.0
python-docx>=1.1.0
python-pptx>=0.6.23
# Vision pipeline (image prep for schematic/electrical extraction)
Pillow>=10.0.0
# Google Drive connector (structure walk + downloads; back-office side)
google-api-python-client>=2.100.0
google-auth>=2.23.0
# PMS provider (YMP REST API)
requests>=2.31.0
# Diagram tools (tools/draw_*.py only)
matplotlib>=3.8.0
# Multi-provider vision (benchmark + per-class routing; back-office side)
google-genai>=1.0.0
openai>=1.60.0
# Dev / tests
pytest>=8.0.0
# Optional (only if OBJECT_STORAGE_PROVIDER=supabase — back-office storage):
# supabase>=2.0.0
pymupdf
opencv-python-headless
